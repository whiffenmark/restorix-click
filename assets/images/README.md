# Add real images here before launch.
# Recommended files:
#   og-image.png       — 1200×630px Open Graph image
#   favicon.ico        — site favicon
#   logo.png           — logo (optional, SVG logo is inline in HTML)
#   hero-bg.jpg        — hero background (optional)
